version: "3.7"
services:
  app-db:
    container_name: galaxy-dashboard-db-mysql
    image: mysql
    restart: no
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_USER: ${DB_USERNAME}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"  # Uncomment to allow empty root password
      # MYSQL_RANDOM_ROOT_PASSWORD: "yes"  # Uncomment to generate a random root password

    volumes:
      - db-dashboard-v2:/var/lib/mysql
volumes:
  db-dashboard-v2:
